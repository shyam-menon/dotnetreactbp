# Initialize Git repository
git init

# Add all files
git add .

# Create first commit
git commit -m "Initial commit: React + .NET starter template"

# Instructions for the next steps
Write-Host "Git repository initialized with initial commit."
Write-Host ""
Write-Host "Next steps:"
Write-Host "1. Create a new repository on GitHub/GitLab/etc."
Write-Host "2. Add remote with: git remote add origin <repository-url>"
Write-Host "3. Push with: git push -u origin main"
